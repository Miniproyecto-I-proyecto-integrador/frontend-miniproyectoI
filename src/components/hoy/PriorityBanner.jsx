import { AlertCircle } from 'lucide-react'
export default function PriorityBanner() { return <div className="priority-banner"><AlertCircle size={18} /><span>Tus gestiones se ordenan por prioridad: primero vencidas, luego las de hoy y finalmente próximas. En cada grupo se prioriza el menor esfuerzo.</span></div> }
